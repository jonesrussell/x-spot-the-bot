{
  "name": "x-spot-the-bot",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite build --watch",
    "build": "npm run generate-icons && tsc && vite build && npm run copy-files",
    "preview": "vite preview",
    "generate-icons": "node --loader ts-node/esm scripts/generate-icons.ts",
    "copy-files": "node --loader ts-node/esm scripts/build.ts"
  },
  "devDependencies": {
    "@types/chrome": "^0.0.287",
    "@types/node": "^22.10.2",
    "canvas": "^3.0.0",
    "ts-node": "^10.9.2",
    "typescript": "^5.7.2",
    "vite": "^6.0.6"
  }
}
